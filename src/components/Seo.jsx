import { useEffect } from 'react'
import { useLocation } from 'react-router-dom'
import { site } from '../config/site'
export function pageMeta(pathname) { const pages = { '/': ['Right Key Properties | Buy, Sell & Rent Property in Pune', 'Right Key Properties helps you discover residential, commercial and investment properties in Pune, with a focus on Pune West.'], '/buy': ['Buy Property in Pune West | Right Key Properties', 'Explore sample homes and commercial property opportunities in Pune West.'], '/rent': ['Rent Property in Pune West | Right Key Properties', 'Explore sample rental homes and commercial spaces in Pune West.'], '/projects': ['Projects in Pune West | Right Key Properties', 'Explore sample upcoming property projects in Pune West.'] }; const [title, description] = pages[pathname] || ['Right Key Properties', 'Property assistance for Pune West.']; return { title, description } }
export default function Seo() { const { pathname } = useLocation(); const meta = pageMeta(pathname); useEffect(() => { document.title = meta.title; document.querySelector('meta[name="description"]')?.setAttribute('content', meta.description); document.querySelector('link[rel="canonical"]')?.setAttribute('href', `${site.domain}/#${pathname}`) }, [meta, pathname]); return null }
